package gabr;

public class Utils {
    // non funziona ovviamente, avrei bisogno dei puntatori, che in Java non esistono
    public void swap(Data d1, Data d2) {
        Data tmp = d1;
        d1 = d2;
        d2 = tmp;
    }
}
